# 🚀 Task 001 – CRUD REST API

## 📖 Overview
This project is a **RESTful API** built to demonstrate **Create, Read, Update, and Delete (CRUD)** operations. It serves as the first technical task for the **Skandavel Webtech Internship**.

The application allows users to manage data stored in a **PostgreSQL** database via structured HTTP requests.

## 🎯 Objective (Why?)
This task was created to:
- Demonstrate proficiency in **Node.js** and **Express.js**.
- Showcase database interaction using **PostgreSQL**.
- Implement standard **REST API architecture**.
- Practice environment configuration and error handling.

## 🛠️ Tech Stack
- **Runtime**: Node.js
- **Framework**: Express.js
- **Database**: PostgreSQL
- **Tools**: Postman (for testing), Dotenv (for configuration)

## ⚙️ Installation & Setup

### 1. Prerequisites
Ensure you have the following installed:
- [Node.js](https://nodejs.org/)
- [PostgreSQL](https://www.postgresql.org/)

### 2. Navigate to the Directory
```bash
cd 001_Task
```

### 3. Install Dependencies
```bash
npm install
```

### 4. Configure Environment
Create a `.env` file in the `001_Task` root directory and add your database credentials:
```env
DB_USER=your_postgres_user
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
PORT=3000
```

### 5. Run the Application
```bash
npm start
```
The server will start on `http://localhost:3000`.

## 👤 Author
**Muhilan**
*Intern Developer @ [Skandavel Webtech](https://www.linkedin.com/company/skandavel)*
*--- [Linkedin Profile](https://www.linkedin.com/in/rcmuhilan/)*

---
*Created for learning and assessment purposes.*
